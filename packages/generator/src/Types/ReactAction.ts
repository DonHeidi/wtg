import type { AssessAction } from './AssessAction.ts'


/**
 * The act of responding instinctively and emotionally to an object, expressing a sentiment.
 */
export interface ReactAction extends AssessAction {

}