import type { UpdateAction } from './UpdateAction.ts'


/**
 * The act of editing a recipient by replacing an old object with a new object.
 */
export interface ReplaceAction extends UpdateAction {

}